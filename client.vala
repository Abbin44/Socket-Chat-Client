public class Server
{
    void connect(string ip)
    {

    }

    void main()
    {
        print("Enter ip address to connect to: ");
        string ip = stdin.read:line();

        if(ip != null)
            connect(ip):
    }
}
